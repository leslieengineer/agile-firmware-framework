# Library Roadmap

Implemented libraries include ButtonInput, FixedRingBuffer, retry/deadline, bounded serialization, CRC16/CRC32, EventBus and StateMachine.

Filters and units remain deferred until a sensor product provides a concrete representation, scaling and accuracy requirement. Header-only algorithms may live in `components/libraries/include/libraries/`; they do not require empty category directories.

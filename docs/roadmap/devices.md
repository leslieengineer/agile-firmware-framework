# Device Roadmap

Only device drivers with source, CMake and host tests belong under `components/devices/`. `sht3x` is the current device sample and remains partial until read/CRC/conversion are implemented.

Deferred categories include actuators, connectivity modules, displays, environment, motion, positioning, power and storage. Promote a concrete device only after its exact part number, bus, electrical constraints and product use case are selected.

# Actuator Devices

Future chip-specific components: relay boards, motor drivers, valve controllers, servo expanders, and LED drivers. Typical devices include PCA9685, DRV8833, and MCP23017.

Use narrow capabilities such as `IGpio`, `IPwm`, `II2c`, or `ISpi`; high-level control policy belongs in a service.

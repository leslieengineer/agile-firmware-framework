# Devices

Chip/device-specific command, register, conversion, CRC and retry behavior over UHAL contracts. Only concrete part-number drivers belong here.

See `../../docs/catalog.md` and `../../docs/roadmap/devices.md`.

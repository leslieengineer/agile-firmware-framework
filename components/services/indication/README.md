# Indication Service

Fixed-capacity priority arbiter for load, command, connectivity, provisioning, OTA and fault indication. Hardware rendering is injected through `IIndicator`; GPIO, PWM and WS2812 details remain product/platform code.

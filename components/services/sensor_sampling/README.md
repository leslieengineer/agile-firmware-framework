# Sensor Sampling Service

Future sampling schedule, warm-up, rate control, and data handoff policy for device drivers. It receives device interfaces and `IClock`; chip commands remain in device components.

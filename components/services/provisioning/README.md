# Provisioning Service

Future first-boot identity, credential setup, configuration validation, and provisioning lifecycle. Secure storage and BLE/Wi-Fi transport remain behind platform/protocol boundaries.

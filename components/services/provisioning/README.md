# Provisioning Service

Non-blocking credential lifecycle over injected portal, validator and config-store ports. It is transport-neutral: SoftAP, HTTP, BLE or serial setup remains in a product adapter. Three validation failures enter failed state while local operation remains available.

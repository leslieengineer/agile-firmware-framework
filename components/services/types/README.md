# Shared Service Value Types

Fixed-capacity value types and capacity budgets shared by Layer 4 services. This component contains data only and must not grow service policy, platform behavior, or vendor dependencies.

# Button Input

Pure fixed-state debounce and press-classification logic. It consumes sampled logical button state plus caller-provided monotonic milliseconds and has no GPIO, ISR, RTOS, or board dependency.

# Libraries

Pure bounded algorithms with no vendor, RTOS or board dependency. Implemented standalone components and shared header-only primitives live here.

See `../../docs/catalog.md` and `../../docs/roadmap/libraries.md`.

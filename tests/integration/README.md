# Integration Tests

Use this directory for checks that require target hardware: pin mapping, I2C pull-ups, UART baud accuracy, RS-485 DE/RE switching, and recovery after a power cycle.

An integration test may use concrete platform adapters, but it should call the same public component APIs as a product.


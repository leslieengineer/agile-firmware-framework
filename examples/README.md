# Examples

- `local_switch/` demonstrates adapter → UHAL → BinarySwitchService → composition using host fakes.
- `../products/env_monitor/` is the existing environment-monitor composition sample.

Examples are intentionally small. Production board, RTOS, persistence and connectivity wiring belongs in product repositories.
